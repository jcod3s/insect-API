const submitData = document.querySelector('#submitInsectData');

    


submitData.addEventListener('click', async function(form1Submit, form2Submit) {
    const form1 = await document.insectImages.submit();
    
    
    //create image file name
    //save name locally
    //upload to s3
    //return s3 file location
    //save as local variable
    //add file name and location to insectInfo

    //submit updated insectInfo data
   
})